import 'package:flutter/material.dart';

class CustomerBio extends StatelessWidget {
  const CustomerBio({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}